# **APTITUDE RFC-03 Trust Evaluation**

**Status:** Draft
**Type:** Cross-pilot baseline RFC
**Scope:** Certificate Trust Establishment
**Target protocols:** 
**Related profiles:** [APTITUDE Trust Specifications](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/)
**Companion RFC:** RFC-04 Trust Revocation

## **Authors**

Andrea Moro (Fondazione Bruno Kessler - Center for Cybersecurity)

## **Reviewers**

---

## **1\. Introduction**

This document defines the **APTITUDE Presentation Profile** for interoperable credential verification across APTITUDE pilots.

It specifies a baseline, cross-pilot profile for the validation of trust artifacts aligned with the [APTITUDE Trust Specifications](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/), the [references](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#references_3) therein, and the ARF.

The purpose of this RFC is to establish a stable starting point for interoperability across APTITUDE pilots by defining:

- impacted roles and components;
- normative requirements for entities involved in the validation of WRPAC and WRPRC certificates;
- interface definitions for the relevant protocols (OCSP, CRL, Status List Token);
- a conformance check catalogue that maps the normative requirements into verifiable checks;
- a test case matrix that maps the conformance checks into executable test cases for the **ITB+ conformance test cases**.

This RFC is intended to support interoperability first. Pilot-specific or sector-specific extensions may be added later, but they should not weaken the baseline requirements defined here.

---

## **2\. Scope**

This RFC profiles presentation under three normative tracks:

- **Trust Anchor Validation** -- profiled for the validation of LoTE and LoTL as defined in ETSI TS 119 615 and [Trust Anchor Validation](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#trust-anchor-validation-process);
- **Authentication** -- profiled for the validation of the WRPAC certificate chain as defined by [Authentication Process](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#authentication-process), ETSI 119 411-8 and RFC 5280;
- **Authorization** -- profiled for the validation of WRPRC certificates as defined by [Authorization Process](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#authorization-process) and ETSI TS 119 475.

This RFC includes baseline requirements for:

- 

This RFC does **not** define:

- sector-specific certificate semantics (already defined in [WRPAC profile](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#wallet-relying-party-access-certificate), [WRPRC profile](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#wallet-relying-party-registration-certificate)) or Trusted List semantics (already defined in [Trusted List](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#list-of-trusted-entities-and-list-of-trusted-lists));
- the lifecycle of the entities and related Trust Artifacts (already defined in [Trust Management](https://aptitude-consortium.github.io/wp2-trust-specifications/pr-46/pr-workspace/trust-management-process/));
- revocation artifacts, semantics and processes (already defined in [Revocation Menchanisms](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#revocation-mechanisms) and [RFC 004](#rfc004));
- pilot-specific UI requirements;
- detailed test assertions; (these are derived into the *ITB+* test suite from the requirements matrix in this document).

---

## **3\. Normative Language**

The key words **SHALL**, **SHALL NOT**, **SHOULD**, **SHOULD NOT**, **MAY**, and **OPTIONAL** in this document are to be interpreted as described in RFC 2119 and RFC 8174.

---

## **4\. Roles and Components**

### **4.1 Wallet Relying Party**

The **<roles:Wallet Relying Party>**, as defined in the APTITUDE glossary and aligned with the ARF notions of Relying Party, Attestation Provider and PID Provider. In this RFC, it is the entity to whom the WRPAC and WRPRC are issued.  

### **4.2 Wallet Unit**

The **Wallet Unit** is the <components:Wallet Unit> as defined in the ARF and APTITUDE glossary, including one or more <components:Wallet Instance|Wallet Instances> on the Holder’s device or environment. In this RFC, the term **Wallet** refers to the Wallet Unit (and its Wallet Instances) when the distinction is not material, acting on behalf of the <roles:Holder> to verify the trustworthiness of a WRP by evaluating trust with the WRPAC and WRPRC received when transacting with a WRP.

### **4.3 Provider of WRPAC**

The **<roles:Provider of WRPAC>**, as defined in the APTITUDE glossary and aligned with the ARF notion of Provider of WRPAC, is the entity issuing WRPAC and the related status Services (CRL endpoint and/or OCSP responder).

### **4.4 Provider of WRPRC**

The **<roles:Provider of WRPRC>**, as defined in the APTITUDE glossary and aligned with the ARF notion of Provider of WRPRC, is the entity issuing WRPRC and the related status Services (Status List Token endpoint).

### **4.5 LoTE Scheme Operator**

The **<roles:LoTE Scheme Operator>, as defined in the APTITUDE glossary and aligned with the ETSI notion of LoTE Scheme Operator, is the entity responsible for the management of the LoTE Trusted List and the inclusion of the Trust Anchors for the applicable entities.

### **4.6 TL Scheme Operator**

The **<roles:TL Scheme Operator>, as defined in the APTITUDE glossary and aligned with the ETSI notion of TL Scheme Operator, is the entity responsible for the management of the Trusted List and the inclusion of the Trust Anchors for the applicable entities.

### **4.7 Registrar**

The **<roles:Registrar>**, as defined in the APTITUDE glossary and aligned with the ARF notion of Registrar, is the entity responsible for the publication and management of the **<role:Registrar>**.

---

## **5\. High-Level Flows**

The high-level flows for validation of WRPAC and WRPRC can be respectively found in [Authentication Sequence Diagram](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#wallet-relying-party-authentication-sequence-diagram) and [Authorization Sequence Diagram](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#authorization-during-issuance).

---

## **6\. Normative Requirements**

This section defines the baseline normative requirements for APTITUDE implementations of the issuance profile.

Unless otherwise stated, the requirements in this section apply to issuance interactions only.

### **6.1 Wallet Requirements**

A Wallet conforming to this RFC SHALL:

- Support ETSI TS 119 615 for Trust Anchor validation;
- Support RFC 5280 for certificate path validation;
- Support [Authorization Validation Procedures](https://aptitude-consortium.github.io/wp2-trust-specifications/latest/deliverable-2.1-trust/#evaluation-procedures) for the WRPRC and Register response validation.


### **6.2 WRP Requirements**

A WRP conforming to this RFC SHALL:

- Possess a valid WRPAC certificate issued by a trusted Provider of WRPAC, and [OPTIONALLY] a valid WRPRC certificate issued by a trusted Provider of WRPRC;

### **6.3 Provider of WRPAC/WRPRC Requirements**

A Provider of WRPAC/WRPRC conforming to this RFC SHALL:

- be listed in the appropriate LoTE together with its Trust Anchor certificate in the `ServiceDigitalIDentity` field;

### **6.4 LoTE/TL Scheme Operator Requirements**

A LoTE Scheme Operator conforming to this RFC SHALL:

- expose a LoTE endpoint as described in section [LoTE Endpoint](#71-lote-endpoint);

A TL Scheme Operator conforming to this RFC SHALL:

- expose a LoTL/TL endpoint as described in section [LoTL/TL Endpoint](#72-lotleums-tl-endpoint);

### **6.5 Registrar Requirements**

A Registrar conforming to this RFC SHALL:

- be listed in the appropriate LoTE together with its Trust Anchor certificate in the `ServiceDigitalIDentity` field;
- expose a Register endpoint as described in section [Register Endpoint](#73-register-endpoint);

---

## **7\. Interface Definitions**

This section defines the logical interfaces for conformance testing. The following interfaces are added on top of these added in RFC001, RFC002.

### **7.1 LoTE Endpoint**

- **Direction**: Wallet to LoTE Scheme Operator.
- **Method**: `GET`

#### **Request**

The Wallet SHALL construct an HTTP `GET` request to retrieve the LoTE associated with the certificate's Trust Anchor.

- **URI Construction**:

  - The request URI is extracted directly from the 
  - No additional query parameters are required.

- **Headers**:

  - `Host`: SHALL contain the hostname from the LoTE URI.
  - `Accept`: SHALL contain the value `application/json`.

#### **Response**

The LoTE Scheme Operator endpoint SHALL respond with the appropriate HTTP status codes and the correctly formatted LoTE data if the request is successful.

- **Status Codes**:

  - `200 OK`: The request was successful, and the LoTE data is included in the response body.
  - `4xx` / `5xx`: Standard HTTP error codes (e.g., `404 Not Found`, `500 Internal Server Error`) SHALL be used only if the server cannot process the HTTP request at the transport level.

- **Headers**:

  - `Content-Type`: SHALL be set to `application/json` for successful responses containing the LoTE data.
  - `Content-Length`: SHOULD be included to indicate the size of the LoTE data in bytes.

- **Body**:

  - The response body SHALL contain the LoTE data in JSON format as specified in ETSI TS 119 612.

### **7.2 LoTL/TL Endpoint**

- **Direction**: Wallet to LoTL/TL Scheme Operator.
- **Method**: `GET`

#### **Request**

The Wallet SHALL construct an HTTP `GET` request to retrieve the LoTL/TL associated with the certificate's Trust Anchor.

- **URI Construction**:

  - The request URI is extracted directly from the 
  - No additional query parameters are required.

- **Headers**:

  - `Host`: SHALL contain the hostname from the LoTL/TL URI.
  - `Accept`: SHALL contain the value `application/xml`.

#### **Response**

The LoTL/TL Scheme Operator endpoint SHALL respond with the appropriate HTTP status codes and the correctly formatted LoTL/TL data if the request is successful.

- **Status Codes**:

  - `200 OK`: The request was successful, and the LoTL/TL data is included in the response body.
  - `4xx` / `5xx`: Standard HTTP error codes (e.g., `404 Not Found`, `500 Internal Server Error`) SHALL be used only if the server cannot process the HTTP request at the transport level.

- **Headers**:

  - `Content-Type`: SHALL be set to `application/xml` for successful responses containing the LoTL/TL data.
  - `Content-Length`: SHOULD be included to indicate the size of the LoTL/TL data in bytes.

- **Body**:

  - The response body SHALL contain the LoTL/TL data in XML format as specified in ETSI TS 119 602.

### **7.3 Register Endpoint**

- **Direction**: Wallet Unit to National Register API.
- **Method**: `GET`
- **Authentication**: None (Open for public access without prior authentication).

The Wallet Unit utilizes the public endpoints of the national common REST API to query and verify the registration status, declared entitlements, and registered intended uses of a WRP.

#### **Request**

The Registry API exposes two public `GET` endpoints that the Wallet Unit may invoke depending on the verification context:

- `GET /wrp` **(Search / List)**: Used to fetch full registry data matching specific query criteria. If no query parameters are provided, it returns the full list of registered WRPs subject to pagination profiles.
- `GET /wrp/check-intended-use` **(Intended Use Check)**: A dedicated, narrowed-down endpoint used to explicitly verify whether a specific transaction profile is registered for a given WRP.

- **URI Construction**:

  - The base request URI is extracted from the registry configuration metadata (e.g., the `registryURI` field or relevant Trust List service locations).
  - For `GET /wrp`, the endpoint supports parameterized queries using combined filters. Supported query parameters include:
    
    - `identifier`: Filter by official business registration number.
    - `legalname`: Filter by official company name.
    - `tradename`: Filter by user-facing trade name.
    - `policy`: Filter by privacy policy URL.
    - `entitlement`: Filter by entitlement type normative URI.
    - `intendeduseidentifier`: Filter by the registrar-assigned intended-use identifier.
    - `credentialformat` / `credentialmeta` / `claimpath`: Filter by specific requested attestation criteria.
    - `providesattestation`: Filter by attestation types provided.
    - `cursor` / `limit`: For cursor-based pagination handling.

  - For `GET /wrp/check-intended-use`, the parameters include:

    - `rpidentifier`: REQUIRED. The identifier of the WRP being checked.
    - `intendeduseidentifier`: OPTIONAL. The registered identifier for the specific intended use.
    - `credentialformat` / `credentialmeta` / `claimpath`: OPTIONAL parameters to check specific data request permissions.
    - `policyurl`: OPTIONAL parameter to check if the explicit privacy policy URL is registered for the WRP.

- **Headers**:

  - `Host`: SHALL contain the hostname from the `ServiceSupplyPoints` URI.
  - `Accept`: SHALL contain the value `application/jwt`.

#### **Response**

- **Status Codes**:

  - `200 OK`: The query was successfully received, evaluated, and resolved. A valid cryptographically signed statement is returned in the response body (even if the search filters result in an empty array or a boolean `false` check).
  - `400 Bad Request`: The request payload or query parameters were structurally invalid or incomplete.
  - `404 Not Found`: Used specifically by `/wrp/{identifier}` or `/wrp/check-intended-use` if the targeted WRP identifier does not exist in the register dataset.
  - `5xx`: Standard HTTP error codes SHALL be used if the registry service cannot process the request at the infrastructure or application layer.

- **Headers**:

  - `Content-Type`: SHALL be set to `application/jwt`.
  - `Content-Length`: SHOULD accurately reflect the size of the DER-encoded OCSP response in bytes.
  - `Cache-Control` / `Expires`: MAY be included to govern HTTP caching behaviors, in case it SHALL align with the `exp` field provided in the internal payload.

- **Body Format**:

  - The HTTP response body SHALL contain a JWS compact serialization string (Media Type: `application/jwt`) electronically signed or sealed by or on behalf of the Registrar.
  - When parsing the decoded JSON payload from a `GET /wrp` response, the statement containing the matching `WalletRelyingParty` data object array SHALL completely exclude the `postalAddress` field (which is restricted to the registrar administration view only). All other data schema parameters (including trade names, entitlements, and intended use structures) are visible as registered.
  - For a `GET /wrp/check-intended-use` response, the decoded JWS payload resolves to a compact boolean representation (`true` or `false`) or its profiled envelope equivalent indicating compliance.

---

## **8\. Privacy and Security Considerations**


### **8.1 Privacy Considerations**


### **8.2 Security Considerations**


---

## **9\. Conformance**

An implementation **conforms to this specification as a Wallet** if it:

1. implements the requirements defined in [Section 6.1](#61-wallet-requirements);
2. supports the interfaces defined in [Section 7](#7-interface-definitions) for all the relevant tracks (CRL, OCSP, TSL);
3. successfully passes all the conformance checks defined in the [Conformance Check Catalogue](#101-conformance-check-catalogue) for the relevant tracks.

An implementation **conforms to this specification as a Provider of WRPAC** if it:

1. implements the requirements defined in [Section 6.2](#62-provider-of-wrpac-requirements);
2. supports the interfaces defined in [Section 7](#7-interface-definitions) for the CRL and OCSP tracks;

An implementation **conforms to this specification as a Provider of WRPRC** if it:

1. implements the requirements defined in [Section 6.3](#63-provider-of-wrprc-requirements);
2. supports the interfaces defined in [Section 7](#7-interface-definitions) for the TSL track;

---

## **10. Wallet-Under-Test Conformance Catalogue and Deployed Test Matrix**

### **10.1 Conformance Check Catalogue**

This catalogue defines the baseline conformance checks that support RFC interoperability claims under [Section 9](#9-conformance). These checks map directly to the normative requirements defined in Sections [6](#6-normative-requirements) and [7](#7-interface-definitions).

#### **10.1.1 Baseline conformance checks**

| Check ID | Conformance Check | What Is Verified At Runtime | Reference |
| --- | --- | --- | --- |
| **CHK-WAL-01** | Trust Anchor Validation | The Wallet validates the incoming Trust Anchor infrastructure strictly adhering to the ETSI TS 119 615 specification rules. | [Section 6.1](#61-wallet-requirements) |
| **CHK-WAL-02** | Certificate Path Validation | The Wallet executes certificate path validation according to RFC 5280 rules for all presented certificates. | [Section 6.1](#61-wallet-requirements) |
| **CHK-WAL-03** | Authorization Validation Procedures | The Wallet processes and verifies the authorization data structures for WRPRC and Registrar responses following the authoritative evaluation procedures. | [Section 6.1](#61-wallet-requirements) |
| **CHK-LOTE-01** | LoTE Request Construction | The Wallet issues an HTTP `GET` request to the extracted LoTE URI, containing a matching `Host` header and an `Accept` header set to `application/json`. | [Section 7.1](#71-lote-endpoint) |
| **CHK-LOTE-02** | LoTE Envelope Validation | The LoTE Scheme Operator response returns an HTTP status code of `200 OK` and a `Content-Type` header strictly matching `application/json`. | [Section 7.1](#71-lote-endpoint) |
| **CHK-LOTE-03** | LoTE Data Format Verification | The retrieved response body contains a structurally valid JSON data object matching the technical format definitions of ETSI TS 119 612. | [Section 7.1](#71-lote-endpoint) |
| **CHK-LOTL-01** | LoTL/TL Request Construction | The Wallet issues an HTTP `GET` request to the extracted LoTL URI, containing a matching `Host` header and an `Accept` header set to `application/xml`. | [Section 7.2](#72-lotltl-endpoint) |
| **CHK-LOTL-02** | LoTL/TL Envelope Validation | The LoTL/TL Scheme Operator response returns an HTTP status code of `200 OK` and a `Content-Type` header strictly matching `application/xml`. | [Section 7.2](#72-lotltl-endpoint) |
| **CHK-LOTL-03** | LoTL/TL Data Format Verification | The retrieved response body contains a structurally valid XML document matching the technical definitions of ETSI TS 119 602. | [Section 7.2](#72-lotltl-endpoint) |
| **CHK-REG-01** | Registry Endpoint Resolution | The Wallet successfully targets either `GET /wrp` (with valid query parameter filters) or `GET /wrp/check-intended-use` using the parameters specified by the context. | [Section 7.3](#73-register-endpoint) |
| **CHK-REG-02** | Registry Request Headers Verification | The Wallet request is verified to contain a `Host` header derived from the `ServiceSupplyPoints` URI and an `Accept` header set to `application/jwt`. | [Section 7.3](#73-register-endpoint) |
| **CHK-REG-03** | Registry Response Envelope Validation | The National Register API handles outcomes gracefully, issuing a `200 OK` code for resolved data, a `400 Bad Request` for invalid schemas, or a `404 Not Found` for absent identifiers, paired with `Content-Type: application/jwt`. | [Section 7.3](#73-register-endpoint) |
| **CHK-REG-04** | Registry Statement Cryptographic Validation | The Wallet verifies that the response body contains a valid JWS compact serialization string signed or sealed with an authorized key on behalf of the Registrar. | [Section 7.3](#73-register-endpoint) |
| **CHK-REG-05** | Registry Data Content Redaction Check | The JWS payload decoded from a `GET /wrp` response is verified to be completely missing the `postalAddress` schema field. | [Section 7.3](#73-register-endpoint) |
| **CHK-REG-06** | Intended Use Status Mapping | The JWS payload decoded from a `GET /wrp/check-intended-use` response resolves cleanly to a compact boolean (`true`/`false`) statement or its profiled envelope equivalent. | [Section 7.3](#73-register-endpoint) |
| **CHK-TRUST-01** | Provider Trust Anchor Mapping | The system asserts that the Provider of WRPAC/WRPRC is listed in the appropriate LoTE and that its certificate matches the `ServiceDigitalIDentity` field. | [Section 6.3](#63-provider-of-wrpacwrprc-requirements) |
| **CHK-TRUST-02** | Registrar Trust Anchor Mapping | The system asserts that the Registrar is explicitly listed in the corresponding LoTE and that its certificate matches the authoritative `ServiceDigitalIDentity` field. | [Section 6.5](#65-registrar-requirements) |

---

### **10.2 Deployed Test Case Matrix**

The test matrix codes are generated by the three tracks defined in this RFC (Trust Anchor Validation, Authentication and Authorization), the test case ID is defined as follows:

#### **10.2.1 Trust Anchor Validation Test Case Matrix**

| Test Case ID | Deployed Scenario / Scope | Request Profile / Variant | Checks Covered | Expected Outcome |
| --- | --- | --- | --- | --- |
| **TAV-LOTE-URL-01a** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | HTTP `GET` request to a valid `OJEU-LoTE-Loc` URL returns a valid, unexpired LoTE payload. | `CHK-WAL-01`<br>`CHK-LOTE-01` | **Success**: `LoTE` variable successfully populated with the downloaded JWT payload; status `200 OK` received. |
| **TAV-LOTE-URL-01b** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | The location pointer check confirms `OJEU-LoTE-Loc == LoTELocation` with synchronized payload contents. | `CHK-WAL-01`<br>`CHK-LOTE-01` | **Success**: Local validation proceeds; no location conflict flags are raised. |
| **TAV-LOTE-URL-02a** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | The runtime variables detect `OJEU-LoTE-Loc != LoTELocation` AND the content downloaded from `OJEU-LoTE-Loc` differs from local `LoTELocation` records. | `CHK-WAL-01`| **Failure**: Aborts processing immediately; returns error string `LoTE_VERIFICATION_FAILED` with sub-status `LoTE_FILE_CONFLICT`. |
| **TAV-LOTE-URL-02b** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | The network layer drops or refuses connection to the designated endpoint, or returns a non-200 transport error (e.g., `404` or `500`). | `CHK-LOTE-01` | **Failure**: Request validation fails; runtime environment triggers infrastructure error fallback path. |
| **TAV-LOTE-URL-02c** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Endpoint returns a payload successfully (`200 OK`) but explicitly violates header requirements by serving a mime-type other than `application/json`. | `CHK-LOTE-02` | **Failure**: Envelope verification aborts due to invalid media structure before processing internal claims. |
| **TAV-LOTE-LOC-01** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Iterate through `SchemeInformationURI` to find `OJEU-Loc` matching entry and establish history count `n`. | `CHK-WAL-01` | **Success**: Trust Anchor migration path established, loop parameter `n` initialized. |
| **TAV-LOTE-LOC-01-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Scan `SchemeInformationURI` where no entry matches the trusted `OJEU-Loc` input. | `CHK-WAL-01` | **Failure**: `LoTE_VERIFICATION_FAILED` with sub-status `OJEU_LOCATION_INPUT_NOT_MATCHING_OJEU_LOCATION_IN_LoTE`. |
| **TAV-LOTE-LOC-02** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Evaluates the condition `OJEU-LoTE-Loc == LoTELocation` AND `LoTE != Content at LoTELocation`. | `CHK-WAL-01` | **Success**: Condition evaluates to `FALSE`; the execution flow transitions smoothly to the next sequential validation step. |
| **TAV-LOTE-LOC-02-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Forces a conflict condition where `OJEU-LoTE-Loc != LoTELocation` AND the downloaded file does not match the cached `LoTELocation` file content. | `CHK-WAL-01` | **Failure**: Validation stops immediately; sets `LoTE-Status` to `LoTE_VERIFICATION_FAILED` and `LoTE-Sub-Status` to `LoTE_FILE_CONFLICT`. |
| **TAV-LOTE-LOC-03** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Forces the condition `OJEU-LoTE-Loc == LoTELocation` AND the working `LoTE` content differs from the content hosted at `LoTELocation`, indicating an update is available. | `CHK-WAL-01` | **Success**: Condition evaluates to `TRUE`; the system updates `OJEU-LoTE-Loc` to the new `LoTELocation` URI and restarts validation. |
| **TAV-LOTE-LOC-04** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | The condition `OJEU-LoTE-Loc == LoTELocation` AND the working `LoTE` content is identical to the content at `LoTELocation` is not met. | `CHK-WAL-01` | **Success**: Condition evaluates to `FALSE`; the execution flow transitions smoothly to the next sequential validation step. |
| **TAV-LOTE-VAL-01** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Cryptographic signature validation of the current LoTE via the public key from `LoTE-Signer-Cert` passes successfully. | `CHK-WAL-01` | **Success**: Signature is valid. System sets `LoTESO-Cert` to `LoTE-Signer-Cert`, populates `LoTESO-Certs-Set` with certificates from the `PointersToOtherLoTE` claim (territory EU) of the current LoTE payload, and proceeds. |
| **TAV-LOTE-VAL-01-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Injected cryptographic signature failure on the current LoTE envelope structure. | `CHK-WAL-01` | **Failure**: Validation stops immediately. Sets `LoTE-Status` to `LoTE_VERIFICATION_FAILED` and `LoTE-Sub-Status` to `LoTE_SIGNATURE_VERIFICATION_FAILED`. |
| **TAV-LOTE-PIV-NO** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | History counter $n=0$ (No Pivots detected). Validation branches around the backward iteration logic. | `CHK-WAL-01` | **Success**: Execution skips intermediate loop steps entirely and transitions directly to Step 8 (Trust Anchor Validation). |
| **TAV-LOTE-PIV-01** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | History counter $n>0$. | `CHK-WAL-01` | **Success**: Sequential processing loops through downloaded files from the designated $i$-th URI history stream. |
| **TAV-LOTE-PIV-02** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Prior sequence `LoTESO-Cert` is verified to be explicitly present within the current `Pivot-Certs-Set` parsed from the target file. | `CHK-WAL-01` | **Success**: Trust link confirmed. System sets the new tracking variable `LoTESO-Cert` to the first certificate entry listed in the Pivot's `x5c` header parameter. |
| **TAV-LOTE-PIV-02-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Injected trust linkage gap where `LoTESO-Cert` is omitted or does not match any certificate inside `Pivot-Certs-Set`. | `CHK-WAL-01` | **Failure**: Validation fails immediately. Sets `LoTE-Sub-Status` to the exact indexed error string: `PIVOT_i-1_SIGNER_CERT_NOT_AUTHENTICATED_BY_PIVOT_i`. |
| **TAV-LOTE-PIV-03** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Cryptographic validation of intermediate `Pivot` payload signature evaluates as valid using the current updated `LoTESO-Cert`. | `CHK-WAL-01` | **Success**: Signature is verified. Loop sequence increments safely backward towards the oldest historical migration Pivot block. |
| **TAV-LOTE-PIV-03-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Cryptographic signature validation fails on the intermediate `Pivot` file during the backward loop. | `CHK-WAL-01` | **Failure**: Validation fails. Sets `LoTE-Status` to `LoTE_VERIFICATION_FAILED` and `LoTE-Sub-Status` to `PIVOT_i_SIGNATURE_VERIFICATION_FAILED`. |
| **TAV-LOTE-PIV-04** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Terminal processing verification. The final `LoTESO-Cert` is matched against the native root array entry inside `OJEU-LoTE-Certs-Set`. | `CHK-WAL-01` | **Success**: Absolute root anchor binding established. Sequence passes into the temporal validation gate. |
| **TAV-LOTE-PIV-04-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Root Trust Anchor mismatch. The final computed signer identity certificate is completely absent from the configured `OJEU-LoTE-Certs-Set`. | `CHK-WAL-01` | **Failure**: Process terminates immediately. Sets `LoTE-Sub-Status` to `PIVOT_n_SIGNER_CERT_NOT_AUTHENTICATED_BY_OJEU`. |
| **TAV-LOTE-EXP-OK** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Current execution system time boundary matches temporal bounds (`current_time` $\le$ `NextUpdate` claim of LoTE). | `CHK-WAL-01` | **Success**: Metadata envelope freshness assertion checks out. Execution proceeds to state preservation phase. |
| **TAV-LOTE-EXP-FAIL** | PID, WRPAC, WRPRC, Register, WIA, WUA, PuB-EAA validation | Expired artifact window check. The target execution timeline exceeds validity constraints (`current_time` $>$ `NextUpdate` claim). | `CHK-WAL-01` | **Failure**: Processing aborts immediately due to an expired metadata timeline. |
| **TAV-LOTL-URL-01a** | QEAA, (PuB-)EAA validation | HTTP `GET` request to a valid `OJEU-LoTE-Loc` URL returns an XML LoTL payload. | `CHK-WAL-01`<br>`CHK-LOTL-01` | **Success**: `LoTL` variable successfully populated with the downloaded XML payload; status `200 OK` received. |
| **TAV-LOTL-URL-01b** | QEAA, (PuB-)EAA validation | The location pointer check confirms `OJEU-LoTL-Loc == LoTLLocation` with synchronized payload contents. | `CHK-WAL-01`<br>`CHK-LOTL-01` | **Success**: Local validation proceeds; no location conflict flags are raised. |
| **TAV-TL-VAL-01** | QEAA, (PuB-)EAA validation | Parses a valid `Authenticated-LOTL` instance to locate the `TSLLocation` field under the `PointersToOtherTSL` element for the target Member State. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Success**: `TSLLocation` is successfully resolved for the targeted `SchemeTerritory`. Execution proceeds to download. |
| **TAV-TL-VAL-02-OK** | QEAA, (PuB-)EAA validation | System issues an HTTP `GET` request to the resolved `TSLLocation` and receives a valid XML document payload. | `CHK-WAL-01`<br>`CHK-LOTL-01`<br>`CHK-LOTL-02` | **Success**: The `EUTL` variable is successfully populated with the raw downloaded XML structure, returning status `200 OK`. |
| **TAV-TL-VAL-02-FAIL** | QEAA, (PuB-)EAA validation | The download connection to `TSLLocation` fails, timeouts, or yields a non-200 envelope code. | `CHK-LOTL-01`<br>`CHK-LOTL-02` | **Failure**: Processing terminates. `EUTL` remains unpopulated, triggering infrastructure transport level error fallbacks. |
| **TAV-TL-VAL-03** | QEAA, (PuB-)EAA validation | Extracts the authoritative `X509Certificates` tuple from the LOTL and extracts the active `ds:X509Certificate` from the XML signature block within the downloaded `EUTL`. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Success**: `EUTL-Certs-Set` is successfully initialized with the reference certificates, and `EUTL-Signer-Cert` is correctly mapped from the `ds:KeyInfo` block. |
| **TAV-TL-VAL-04** | QEAA, (PuB-)EAA validation | Executes cryptographic XML digital signature block checks on the `EUTL` payload structure using the extracted `EUTL-Signer-Cert`. | `CHK-WAL-01` | **Success**: Signature validation completes as structurally successful; execution gate transitions directly to the certificate binding check. |
| **TAV-TL-VAL-04-FAIL** | QEAA, (PuB-)EAA validation | Injected signature corruption structural integrity invalidity inside the downloaded `EUTL` XML file envelope. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Failure**: Validation fails immediately. Sets `EUTL-Status` to `EUTL_VERIFICATION_FAILED` and `EUTL-Sub-Status` to `EUTL_SIGNATURE_VERIFICATION_FAILED`. |
| **TAV-TL-VAL-05** | QEAA, (PuB-)EAA validation | Asserts identity check confirming active `EUTL-Signer-Cert` is explicitly present within the baseline `EUTL-Certs-Set` array derived from the LOTL. | `CHK-WAL-01` | **Success**: Identity matches. Signer authenticity confirmed; process moves forward to freshness assertion gates. |
| **TAV-TL-VAL-05-FAIL** | QEAA, (PuB-)EAA validation | The signature is valid, but the extracted `EUTL-Signer-Cert` is entirely missing from the reference `EUTL-Certs-Set`. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Failure**: Process terminates immediately. Sets `EUTL-Status` to `EUTL_VERIFICATION_FAILED`, purges running context data by setting `Authenticated-LOTL` to `null`, and sets `EUTL-Sub-Status` to `EUTLSO_SIGNER_CERT_NOT_AUTHENTICATED_BY_LOTL`. |
| **TAV-TL-VAL-06** | QEAA, (PuB-)EAA validation | Asserts the temporal constraint checking that the runtime context timeline satisfies $current\_time \le NextUpdate$ element value. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Success**: Envelope freshness check is satisfied; sequence transitions into the final transaction state commit block. |
| **TAV-TL-VAL-06-FAIL** | QEAA, (PuB-)EAA validation | The current date/time evaluation overruns boundary conditions ($current\_time > NextUpdate$ element value). | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Failure**: Processing is rejected. Sets `EUTL-Status` to `EUTL_VERIFICATION_FAILED` and `EUTL-Sub-Status` to `WARNING_EUTL_NEXTUPDATE_PASSED`. |
| **TAV-TL-VAL-07** | QEAA, (PuB-)EAA validation | Executes state transition logic after all sequential validation assertions evaluate as valid. | `CHK-WAL-01`<br>`CHK-LOTL-02` | **Success**: Commits working state parameters to active runtime layers: Sets `Authenticated-EUTL` to the validated `EUTL` document structure, updates `EUTL-Status` to `EUTL_VERIFICATION_PASSED`, and sets `EUTL-Sub-Status` to an empty list. |

**Note**: The test vase matrix for the LOTL validation is the same as the one for the LoTE.


#### **10.2.2 Authentication Test Case Matrix**

| Test Case ID | Deployed Scenario | Request Profile / Variant | Checks Covered | Expected Outcome |
| --- | --- | --- | --- | --- |
| **AUTHN-PATH-01** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Happy Path: Valid Chain Construction | Verifies a continuous, unbroken sequence $C_1 \dots C_n$ where $C_1$ is issued by the Trust Anchor, $C_n$ is the target WRPAC, and the issuer of $C_{i}$ matches the subject (`working_issuer_name`) of $C_{i-1}$. | **Success**: Path correctly constructed and mapped to inputs. |
| **AUTHN-PATH-01-FAIL** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Negative Path: Broken Certification Link | Verifies error handling when the issuer name of $C_i$ **does NOT match** the expected `working_issuer_name` (subject of $C_{i-1}$). | **Failure**: Path validation aborts with an invalid path status. |
| **AUTHN-INIT-01** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Algorithm Initialization Boundary. Verifies initial setup of state parameters (`valid_policy_tree`, `explicit_policy` = $n+1$, `inhibit_any_policy` = $n+1$, `working_public_key` = Trust Anchor PK, `max_path_length` = $n$). | `CHK-WAL-02` | **Success**: State variables successfully initialized for the processing loop. |
| **AUTHN-VAL-01** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Cryptographic Signature Validation. Verifies the signature of certificate $C_i$ using the `working_public_key` and associated public key parameters. | `CHK-WAL-02` | **Success**: Signature passes structural and cryptographic integrity checks. |
| **AUTHN-VAL-01-FAIL** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Cryptographic Signature Failure. Verifies error handling when a certificate signature check fails using the `working_public_key`. | `CHK-WAL-02` | **Failure**: Validation fails immediately with an abort signal. |
| **AUTHN-VAL-02** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Happy Path: Within Validity Window | Verifies that the processing `current_time` falls strictly inside the `notBefore` and `notAfter` temporal validity windows of $C_i$. (`CHK-WAL-02`) | **Success**: Temporal validity check passes; path processing transitions to policy evaluation. |
| **AUTHN-VAL-02-FAIL** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Negative Path: Outside Validity Window | Verifies error handling when the processing `current_time` falls outside the `notBefore` (not yet valid) or `notAfter` (expired) windows of $C_i$. (`CHK-WAL-02`) | **Failure**: Validation aborts immediately with an expired or not-yet-valid status signal. |
| **AUTHN-VAL-03** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Happy Path: Valid Policy Matching | Verifies that when the `certificatePolicies` extension is present, the algorithm successfully executes branching, qualifier mapping, and valid pruning of the `valid_policy_tree`. | **Success**: `valid_policy_tree` is updated and maintained with valid policy nodes. |
| **AUTHN-VAL-03-FAIL** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Negative Path: Missing / Non-Matching Policies | Verifies that if the `certificatePolicies` extension is missing or yields no matching policy OIDs, the state variable `valid_policy_tree` is explicitly set to `NULL`. | Tree transitions to `NULL`. *(Does not abort until evaluated by the following gate)*. |
| **AUTHN-VAL-04** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Happy Path: Policy Gate Satisfied | Verifies that path validation safely proceeds when the structural constraint is met (i.e., `explicit_policy > 0` OR `valid_policy_tree` is NOT `NULL`). | **Success**: Verification constraint passes; loop transitions to intermediate CA checks. |
| **AUTHN-VAL-04-FAIL** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Negative Path: Policy Gate Constraint Failure | Verifies that the validation loop immediately halts if a certificate breaks the policy intersection rule (i.e., `explicit_policy == 0` AND `valid_policy_tree` is `NULL`). | **Failure**: Validation aborts immediately with a policy constraint violation error. |
| **AUTHN-VAL-05** | OpenID4VP / ISO 18013-5 / OpenID4VCI | For intermediate certificates ($i < n$), verifies that the `basicConstraints` extension is present and the `cA` boolean is set to TRUE. | `CHK-WAL-02` | **Failure**: Validation aborts if an intermediate certificate lacks the `cA=TRUE` designation. |
| **AUTHN-VAL-06** | OpenID4VP / ISO 18013-5 / OpenID4VCI | For intermediate certificates ($i < n$), verifies that if the `keyUsage` extension is present, the `keyCertSign` bit is explicitly asserted. | `CHK-WAL-02` | **Failure**: Validation aborts if the `keyCertSign` bit is absent on intermediate CAs. |
| **AUTHN-VAL-07** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Verifies that the running `max_path_length` parameter is $> 0$ and handles `pathLenConstraint` modifications correctly.  | `CHK-WAL-02` | **Failure**: Validation aborts if the depth of the incoming path exceeds the permitted length. |
| **AUTHN-VAL-08** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Verifies proper state handover for the next loop execution (updating parameters to current $C_i$ data, decrementing counters). | `CHK-WAL-02` | **Success**: State parameters cleanly transition to represent intermediate CA $C_i$. |
| **AUTHN-VAL-9** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Evaluates final target certificate condition checking if `explicit_policy > 0` OR `valid_policy_tree` is NOT NULL. | `CHK-WAL-02` | **Success**: Returns `Path Validation SUCCESSFUL` status. |
| **AUTHN-VAL-10** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Evaluates wrap-up conditions when neither policy rule parameters are fulfilled after target processing. | `CHK-WAL-02` | **Failure**: Returns `Path Validation FAILED` status. |
| **AUTHN-REV-01** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Assesses when a certificate contains both `noRevAvail` and `ETSIValAssuredCertMod` extensions. | `CHK-WAL-02` | **Success**: Revocation checking is safely bypassed based solely on validity periods. |
| **AUTHN-REV-02** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Verifies CRL timeframe validity, matches certificate issuer fields, and confirms `issuingDistributionPoint` constraints. | `CHK-WAL-02` | **Success**: CRL profile structures match target environment properties. |
| **AUTHN-REV-03** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Validates the CRL digital signature via the issuer public key and verifies that the `cRLSign` bit is present. | `CHK-WAL-02` | **Success**: CRL cryptographic signature confirmed good. |
| **AUTHN-REV-04** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Performs lookup for the certificate's serial number within the `revokedCertificates` list. | `CHK-WAL-02` | **Failure**: Returns certificate status as `revoked`. |
| **AUTHN-REV-05** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Assesses if the `responseStatus` is `successful (0)` and `responseType` matches `id-pkix-ocsp-basic`. | `CHK-WAL-02` | **Success**: Structural parsing confirms a valid basic OCSP envelope. |
| **AUTHN-REV-06** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Validates basic responder signatures and verifies that `responderID` and `CertID` hash fields map to the target certificate. | `CHK-WAL-02` | **Success**: Signature and identifier matching successfully pass verification. |
| **AUTHN-REV-07** | OpenID4VP / ISO 18013-5 / OpenID4VCI | Evaluates the specific `certStatus` value mapped inside the parsed `SingleResponse` structure. | `CHK-WAL-02` | **Failure**: Returns certificate status as `revoked` or `unknown` based on the payload. |

#### **10.2.3 Authorization Test Case Matrix**

| Test Case ID | Deployed Scenario | Request Profile / Variant | Checks Covered | Expected Outcome |
| --- | --- | --- | --- | --- |
| **AUTHZ-WRPRC-01** | Issuance / Presentation (Remote Flow) | Remote JWT Envelope Verification | Verifies valid processing of a remote registration token where `typ` is strictly verified as `rc-wrp+jwt`, the signature and path chain resolve successfully to a Trust Anchor from the LoTE, and temporal/coherence constraints pass. | **Success**: WRPRC validated successfully; internal authorization context populated. |
| **AUTHZ-WRPRC-02** | Presentation (Proximity Flow) | Proximity CWT Envelope Verification | Verifies valid processing of a proximity token where `typ` is strictly verified as `rc-wrp+cwt`, the signature and cryptographic chain resolve to an approved WRPRC Provider Trust Anchor from the LoTE, and temporal/coherence constraints pass. | **Success**: WRPRC validated successfully; internal authorization context populated. |
| **AUTHZ-WRPRC-03** | Issuance / Presentation | Format / Algorithm Failure | Verifies error handling when the token format/type is unrecognized (e.g., missing or corrupted `typ` parameter) or when a deprecated or prohibited signature algorithm (like `"none"`) is specified. | **Failure**: Validation fails with `CERTIFICATE_INVALID` status; loop bypasses certificate processing and immediately falls back to the Register Validation Procedure. |
| **AUTHZ-WRPRC-04** | Issuance / Presentation | Cryptographic Chain / Trust Anchor Failure | Verifies error handling when the WRPRC structure contains an invalid cryptographic signature, a broken certificate path link, or when the issuing Provider's Trust Anchor cannot be resolved within the authoritative LoTE. | **Failure**: Validation fails with `CERTIFICATE_INVALID` status; loop bypasses certificate processing and immediately falls back to the Register Validation Procedure. |
| **AUTHZ-WRPRC-05** | Issuance / Presentation | Temporal or Revocation Failure | Verifies error handling when the current transaction time falls outside the token's active temporal window (`iat` / `exp`) or when the embedded `status` field explicitly signals a revoked status. | **Failure**: Validation fails with `CERTIFICATE_INVALID` status; loop bypasses certificate processing and immediately falls back to the Register Validation Procedure. |
| **AUTHZ-WRPRC-06** | Issuance / Presentation | Contextual Coherence Failure | Verifies error handling when a cryptographically valid WRPRC contains subject fields or declared transaction attributes that structurally conflict with the current active deployment context or scenario. | **Failure**: Validation fails with `CERTIFICATE_INVALID` status; loop bypasses certificate processing and immediately falls back to the Register Validation Procedure. |
| **AUTHZ-REGQ-01** | Issuance / Presentation | `GET /wrp` Query Generation | Extraction of Registry URI from metadata (`verifier_info` / `issuer_info.registry_uri`). Formulates query string containing parameters like `identifier`, `legalname`, or `intendeduseidentifier`. | **Success**: Well-formed HTTP `GET` query correctly targeted at the public collection endpoint with appropriate parameters. |
| **AUTHZ-REGQ-02** | Presentation | `GET /wrp/check-intended-use` | Extraction of Registry URI and formulation of query string with mandatory `rpidentifier` and accompanying transaction parameters (`intendeduseidentifier`, `policyurl`). | **Success**: Well-formed narrow-scope HTTP `GET` query dispatched to check the validity of a target transaction profile. |
| **AUTHZ-REGQ-02-FAIL** | Issuance / Presentation | Malformed Query Profile | Handles scenarios where the required registry identifier parameters are missing from the transaction profile or structurally corrupt, or when the Base Registry URI cannot be resolved. | **Failure**: Execution environment short-circuits the query phase, logs a request formation anomaly, and yields an internal query configuration error. |
| **AUTHZ-REGR-01** | Issuance / Presentation | JWS Parsing & Context Normalization | Receives HTTP `200 OK` with an `application/jwt` content-type. Validates the JWS compact serialization envelope, parses the JSON payload, and maps it to the internal WRPRC model. | **Success**: Registry statement accepted; internal ecosystem authorization context populated and forwarded for entity binding. |
| **AUTHZ-REGR-01-FAIL** | Issuance / Presentation | JWS Envelope Cryptographic Failure | Evaluates response when the JWS cryptographic signature fails verification or uses a forbidden signature algorithm, indicating structural tampering. | **Failure**: Rejects payload instantly; throws a cryptographic verification anomaly and prevents registration context extraction. |
| **AUTHZ-REGR-01-MIS** | Issuance / Presentation | Context / Pertinence Mismatch | Evaluates response when the JWS payload is cryptographically valid but the enclosed `identifier` or claims do not match the expected subject identifier or requested intended use. | **Failure**: Rejects payload due to context mismatch; context is dropped, preventing registration binding. |
| **AUTHZ-REGR-01-404** | Issuance / Presentation | Unregistered Target Handling | Handles an HTTP `404 Not Found` response code returned by the `/wrp/{identifier}` or `/wrp/check-intended-use` endpoints. | **Failure**: Correctly interprets the error code as a definitive "Target Entity Not Registered" state, halting authorization steps. |
| **AUTHZ-BIND-01** | Issuance | Attestation Provider Matching | Checks that the Attestation Provider (AP) identity extracted from the authenticated X.509 certificate perfectly matches the `sub` claim inside the WRPRC and the official `identifier` field in the Registrar's registry dataset. | **Success**: Structural binding confirmed; the authorization processing context proceeds to entitlement verification. |
| **AUTHZ-BIND-01-FAIL** | Issuance | Attestation Provider Mismatch | Evaluates error handling when there is any structural mismatch between the authenticated certificate subject identity, the WRPRC `sub`, or the `registrar_dataset.identifier`. | **Failure**: Aborts authorization processing immediately with an explicit `BINDING_FAILED` status. |
| **AUTHZ-BIND-02** | Presentation | Direct RP Identity Binding | Verifies that the authenticated WRPAC subject identifier perfectly matches both the protocol-claimed Relying Party identifier (e.g., client ID) and the corresponding `sub` field of the validated WRPRC payload. | **Success**: Structural binding confirmed; direct transaction flow authorized to proceed. |
| **AUTHZ-BIND-02-FAIL** | Presentation | Direct RP Identity Mismatch | Evaluates error handling when the authenticated WRPAC identity fails to cross-reference with the protocol assertions or the WRPRC statement payload. | **Failure**: Aborts transaction processing immediately with an explicit `BINDING_FAILED` status. |
| **AUTHZ-BIND-03** | Presentation | Intermediary Association Confirmed | Detects a Relying Party Intermediary (RPI) deployment profile. Correctly extracts the `intermediary.sub` claim and successfully validates that the association between the intermediary and the target client is explicitly authorized via the WRPRC rules or an online Register check. | **Success**: Intermediary binding confirmed; proxy transaction authorized to proceed. |
| **AUTHZ-BIND-03-FAIL** | Presentation | Intermediary Association Rejection | Evaluates error handling when an intermediary scenario is identified but the structural association rules or online registry profiles explicitly reject or fail to prove an authorized link between the proxy and the target entity. | **Failure**: Aborts transaction processing immediately with an explicit `INTERMEDIARY_NOT_AUTHORIZED` status. |
| **AUTHZ-ENT-01** | Issuance | Valid Provider Entitlement Matching | Extracts the `entitlements` array from the authorized statement and verifies that the normative URI explicitly matches the expected cryptographic role profile (e.g., `urn:eu:eudi:role:pid-provider` or `urn:eu:eudi:role:qeaa-provider`). | **Success**: Provider role successfully verified; the authorization engine permits the issuance loop to continue. |
| **AUTHZ-ENT-01-FAIL** | Issuance | Missing or Incorrect Provider Role | Evaluates error handling when the `entitlements` array is absent, empty, or contains values that do not intersect with the required provider role needed to issue the targeted attestation. | **Failure**: Aborts the onboarding or renewal flow immediately, returning an explicit `WRONG_ENTITLEMENT` status. |
| **AUTHZ-ENT-02** | Presentation | Valid Service Provider Matching | Evaluates the `entitlements` array within the relying party's validated authorization context to confirm it asserts a valid and authorized `Service_Provider` URI. | **Success**: Relying Party entitlement successfully confirmed; transaction transitions to disclosure policy evaluation. |
| **AUTHZ-ENT-02-FAIL** | Presentation | Invalid Service Provider Profile | Evaluates error handling when a Relying Party requests credential presentation but lacks the general or specific `Service_Provider` entitlement string within its registered attributes. | **Failure**: Terminates the active presentation transaction immediately, returning an explicit `WRONG_ENTITLEMENT` status code to prevent data leakage. |
| **AUTHZ-ATT-01** | Issuance | Valid Issuance Attestation Match | Performs an exact match verification confirming that the requested attestation type identifier (such as the `vct` value for SD-JWT VC or the `doctype` for ISO/IEC 18013-5 mDL) exists inside the provider's registered `providesAttestations[]` data array. | **Success**: Attestation type matched successfully; the authorization sequence proceeds to user consent or issuance generation. |
| **AUTHZ-ATT-01-FAIL** | Issuance | Unregistered Issuance Request | Evaluates error handling when a provider requests or attempts to issue an attestation type that is absent, misspelled, or structurally mismatched against the entries within their authoritative `providesAttestations[]` profile. | **Failure**: Aborts the transaction immediately, returning an explicit `ATTESTATION_TYPE_NOT_REGISTERED` status. |
| **AUTHZ-ATT-02** | Presentation | Valid Presentation Query Match | Evaluates the requested presentation criteria (extracted from the `dcql_query.credentials` or `itemsRequest` arrays) and verifies that every requested credential type strictly maps to an authorized entry in the Relying Party's registered `credentials[]` array. | **Success**: Presentation data scope validated successfully; transaction proceeds to disclosure evaluation. |
| **AUTHZ-ATT-02-FAIL** | Presentation | Scope Overrun / Unregistered Query | Evaluates error handling when a Relying Party issues a presentation request or DCQL query containing credential formats, types, or namespaces that are not explicitly authorized inside their registered `credentials[]` array. | **Failure**: Terminates the presentation transaction immediately to prevent unauthorized data collection, returning an explicit `ATTESTATION_TYPE_NOT_REGISTERED` status code. |
| **AUTHZ-EDPS-01** | Issuance | Retrieval of EDP from Credential Issuer Metadata (`credential_configurations_supported`) and local storage upon User confirmation. | `CHK-WAL-03` | **Success**: EDP rule set successfully cached for future presentation matching. |
| **AUTHZ-ATT-01** | Issuance | Valid Issuance Attestation Match | Performs an exact match verification confirming that the requested attestation type identifier (such as the `vct` value for SD-JWT VC or the `doctype` for ISO/IEC 18013-5 mDL) exists inside the provider's registered `providesAttestations[]` data array. | **Success**: Attestation type matched successfully; the authorization sequence proceeds to user consent or issuance generation. |
| **AUTHZ-ATT-01-FAIL** | Issuance | Unregistered Issuance Request | Evaluates error handling when a provider requests or attempts to issue an attestation type that is absent, misspelled, or structurally mismatched against the entries within their authoritative `providesAttestations[]` profile. | **Failure**: Aborts the transaction immediately, returning an explicit `ATTESTATION_TYPE_NOT_REGISTERED` status. |
| **AUTHZ-ATT-02** | Presentation | Valid Presentation Query Match | Evaluates the requested presentation criteria (extracted from the `dcql_query.credentials` or `itemsRequest.nameSpaces` arrays) and verifies that every requested credential type strictly maps to an authorized entry in the Relying Party's registered `credentials[]` array. | **Success**: Presentation data scope validated successfully; transaction proceeds to disclosure evaluation. |
| **AUTHZ-ATT-02-FAIL** | Presentation | Scope Overrun / Unregistered Query | Evaluates error handling when a Relying Party issues a presentation request or DCQL query containing credential formats, types, or namespaces that are not explicitly authorized inside their registered `credentials[]` array. | **Failure**: Returns an `ATTESTATION_TYPE_NOT_REGISTERED` status code. |
| **AUTHZ-ATT-02-OVERRIDE** | Presentation | Override Path: Scope Overrun with User Consent | Evaluates behavior when an unauthorized credential type is requested. The system flags the anomaly, halts automatic processing, and prompts the user with an explanatory dialog. The user explicitly triggers an override to authorize the disclosure. | **Success (Via Override)**: The structural registration block is overridden by runtime user intent; the application populates the authorization context and proceeds with the presentation. |
| **AUTHZ-EDPV-01** | Presentation | Authorized Relying Party Match | Matches the Relying Party's authenticated subject Distinguished Name (DN) or registered entitlement URIs against the locally stored EDP `authorized_parties` policy array. | **Success**: Outputs `EDP_SATISFIED`; transaction proceeds smoothly to user data disclosure approval without structural exceptions. |
| **AUTHZ-EDPV-01-FAIL** | Presentation | Strict Party Rejection | Evaluates a scenario where the RP attributes fail to match the `authorized_parties` list, and the user **rejects** the resulting exception prompt. | **Failure**: Outputs `EDP_NOT_SATISFIED` internally, aborting the presentation flow and returning a terminal `NOT_AUTHORIZED` status code. |
| **AUTHZ-EDPV-01-OVERRIDE** | Presentation | Override Path: Relying Party Violation Overridden | Evaluates a scenario where the RP fails the matching criteria. The Wallet Instance halts automated processing, triggers an intermediate warning prompt, and the user explicitly bypasses the block. | **Success (Via Override)**: The structural policy block is overridden by direct user consent; the application populates the runtime authorization context. |
| **AUTHZ-EDPV-02** | Presentation | Trusted Root Match | Matches the issuer DN and certificate serial number of the root or intermediate access certificate against the EDP `trusted_roots` constraint array. | **Success**: Outputs `EDP_SATISFIED`; the cryptographic root policy check passes and transitions to user confirmation. |
| **AUTHZ-EDPV-02-FAIL** | Presentation | Strict Root Rejection | Evaluates the system when the certificate path roots do not intersect with the EDP `trusted_roots` list, and the user **declines** to grant a safety exception. | **Failure**: Outputs `EDP_NOT_SATISFIED` internally, aborting the presentation flow and returning a terminal `NOT_AUTHORIZED` status code. |
| **AUTHZ-EDPV-02-OVERRIDE** | Presentation | Override Path: Root Violation Overridden | Evaluates the system when a root authority violation occurs. The interface presents an explanatory security warning dialog to the user, and the user **manually signs off/overrides the alert**. | **Success (Via Override)**: The structural authority mismatch is bypassed by explicit runtime user intent, forcing the authorization sequence to proceed. |