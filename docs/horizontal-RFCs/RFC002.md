# **APTITUDE RFC-02 Presentation Profile**

**Status:** Draft  
**Type:** Cross-pilot baseline RFC  
**Scope:** Credential Presentation / Verification  
**Target protocols:** OpenID4VP v1.0  
**Related profiles:** HAIP (where applicable)  
**Companion RFC:** RFC-01 Issuance Profile

---

## **1\. Introduction**

This document defines the **APTITUDE Presentation Profile** for interoperable credential verification across APTITUDE pilots.

It specifies a baseline, cross-pilot profile for presentation and verification of verifiable credentials, aligned with the Architecture and Reference Framework (ARF), and based on **OpenID for Verifiable Presentations (OpenID4VP) v1.0**. Where applicable, the profile incorporates relevant choices from the **OpenID4VC High Assurance Interoperability Profile (HAIP)**.

The purpose of this RFC is to establish a stable starting point for interoperability across APTITUDE pilots by defining:

* common presentation and verification requirements  
* roles and interfaces between wallets and verifiers  
* baseline security and privacy expectations  
* support for same-device and cross-device presentation patterns  
* a requirements baseline that can be mapped into corresponding **ITB+ conformance test cases**

This RFC is intended to support interoperability first. Pilot-specific or sector-specific extensions may be added later, but they should not weaken the baseline requirements defined here.

---

## **2\. Scope**

This RFC defines the conformance profile for **credential presentation and verification** in APTITUDE.

It covers requirements for:

* **Wallet Units / Wallets** acting on behalf of the Holder  
* **Verifiers / Relying Parties** initiating presentation requests and validating responses

This profile includes the following baseline features:

* OpenID4VP v1.0  
* signed presentation requests where required by the selected profile  
* support for same-device and cross-device presentation flows  
* support for wallet invocation through QR codes, links, or other supported wallet invocation methods  
* presentation of credentials using formats supported by the verifier and wallet  
* verifier-side validation of presentation responses  
* holder consent and selective release of attributes where supported by the credential format

This RFC does **not** define:

* sector-specific credential semantics  
* trust framework governance rules beyond baseline interoperability assumptions  
* pilot-specific UI requirements  
* detailed test assertions; these are derived into the ITB+ test suite from the requirements matrix in this document

---

## **3\. Normative Language**

The key words **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, **MAY**, and **OPTIONAL** in this document are to be interpreted as described in RFC 2119\.

---

## **4\. Roles and Components**

This RFC uses the following roles:

### **4.1 Holder**

The natural person or legal representative controlling the wallet and authorising the presentation of credentials.

### **4.2 Wallet Unit / Wallet**

An application or component acting on behalf of the Holder to manage credentials and respond to presentation requests.

### **4.3 Verifier**

An entity requesting verifiable presentations, validating the response, and making an authorisation or business decision based on the outcome.

### **4.4 Verifier Backend**

The server-side component that creates presentation requests, receives presentation responses, validates them, and returns the result to the relying application.

### **4.5 Relying Application**

The user-facing application, service, or workflow in which credential verification is performed.

---

## **5\. Protocol Overview**

The APTITUDE Presentation Profile is based on **OpenID4VP v1.0** as the baseline presentation protocol.

Where applicable, pilots may adopt HAIP-aligned choices to improve interoperability and assurance, including:

* signed request objects  
* use of verifier metadata  
* strong nonce handling  
* audience restriction  
* cryptographic proof binding  
* selective disclosure support for compatible credential formats

At a high level, the presentation process is:

1. The Verifier creates a Presentation Request.  
2. The Wallet is invoked through a supported method.  
3. The Wallet validates the request.  
4. The Holder reviews the request and provides consent.  
5. The Wallet generates a presentation response.  
6. The Wallet submits the presentation response to the Verifier.  
7. The Verifier validates the response and returns the outcome.

APTITUDE supports both:

* **same-device presentation**, where the wallet is invoked on the same device as the relying application, and  
* **cross-device presentation**, where the request is transferred across devices, typically using QR or a link-based handoff.

Unlike stricter profiles, APTITUDE does not mandate one specific invocation URI scheme as the only allowed option. The profile requires support for interoperable invocation methods suitable for same-device and cross-device use.

---

## **6\. High-Level Flows**

### **6.1 Same-Device Presentation Flow**

#### **6.1.1 Presentation Request Creation**

The Verifier prepares a Presentation Request containing the information needed by the Wallet to determine what credentials and claims are requested, along with the parameters needed to produce a valid response.

The request SHOULD include, as applicable:

* verifier identifier  
* nonce  
* audience or equivalent recipient binding  
* requested credential types or claims  
* disclosure constraints  
* response endpoint information  
* validity and expiry parameters

Where the selected profile requires signed requests, the Presentation Request MUST be integrity protected.

#### **6.1.2 Wallet Invocation**

The Wallet is invoked using a supported same-device method, such as:

* an app-to-app link  
* a deep link  
* a platform-supported wallet invocation mechanism  
* another interoperable wallet invocation approach supported by the ecosystem

#### **6.1.3 Wallet Validation**

The Wallet MUST validate the received Presentation Request before presenting it to the Holder.

Validation SHOULD include, as applicable:

* request syntax and completeness  
* signature or integrity protection, where present or required  
* nonce presence and freshness  
* audience or recipient binding  
* expiry validity  
* supported credential query semantics  
* compatibility with the Wallet’s supported credential formats

Invalid or malformed requests MUST be rejected.

#### **6.1.4 Holder Consent**

The Wallet MUST present the request information to the Holder in a transparent way.

The Wallet SHOULD display, where applicable:

* the Verifier identity  
* the purpose or context of the request  
* the requested credential(s)  
* the requested claims or attributes  
* selective disclosure details, where supported

The Holder MUST explicitly approve the release before a presentation is generated.

#### **6.1.5 Presentation Generation**

After consent, the Wallet generates the presentation response in accordance with OpenID4VP and the credential format in use.

This MAY include:

* a `vp_token`  
* one or more verifiable presentations or credential-derived proofs  
* disclosures, where selective disclosure is supported  
* holder-binding or proof-of-possession material, where required  
* nonce and audience binding data

#### **6.1.6 Presentation Submission**

The Wallet submits the presentation response to the Verifier using the response mechanism defined by the request and supported by the protocol.

Submission MAY be direct from the Wallet to the Verifier backend or may involve an intermediate browser or platform handoff, depending on the flow.

#### **6.1.7 Result Handling**

The Verifier validates the response and returns an outcome.

The outcome SHOULD clearly indicate:

* successful verification  
* verification failure  
* user cancellation  
* unsupported request or unsupported credential format  
* expired or invalid request  
* other protocol or validation errors

The Wallet and/or relying application SHOULD display an understandable outcome to the Holder.

---

### **6.2 Cross-Device Presentation Flow**

#### **6.2.1 Presentation Request Creation and Display**

The Verifier creates the Presentation Request and makes it available through a cross-device transfer mechanism, typically by encoding reference data or invocation data in a QR code or equivalent.

#### **6.2.2 Wallet Invocation via QR, Link, or Equivalent**

The Holder uses a second device to invoke the Wallet and retrieve the Presentation Request.

APTITUDE requires support for cross-device presentation through interoperable means such as:

* QR-based transfer  
* link-based transfer  
* other supported wallet invocation methods

#### **6.2.3 Wallet Validation**

The same validation rules as in Section 6.1.3 apply.

#### **6.2.4 Holder Consent**

The same holder consent requirements as in Section 6.1.4 apply.

#### **6.2.5 Presentation Generation**

The same presentation generation requirements as in Section 6.1.5 apply.

#### **6.2.6 Presentation Submission**

The Wallet submits the presentation response to the Verifier using the mechanism defined by the request and supported by the verifier.

#### **6.2.7 Result Handling**

The Verifier validates the presentation and returns the outcome to the relying application and/or Wallet, as appropriate for the flow.

---

## **7\. Normative Requirements**

### **7.1 Wallet Requirements**

A Wallet conforming to this RFC MUST:

**APT-PRES-WALLET-01**  
Support OpenID4VP v1.0 for credential presentation.

**APT-PRES-WALLET-02**  
Support same-device presentation flows.

**APT-PRES-WALLET-03**  
Support cross-device presentation flows.

**APT-PRES-WALLET-04**  
Support invocation through interoperable methods suitable for the deployment context, including QR and/or link-based invocation where applicable.

**APT-PRES-WALLET-05**  
Validate the Presentation Request before presenting it to the Holder.

**APT-PRES-WALLET-06**  
Reject malformed, expired, or otherwise invalid Presentation Requests.

**APT-PRES-WALLET-07**  
Present the requested credential and claim release information to the Holder in a transparent way.

**APT-PRES-WALLET-08**  
Require explicit Holder consent before releasing data.

**APT-PRES-WALLET-09**  
Generate a presentation response conforming to OpenID4VP and the relevant credential format in use.

**APT-PRES-WALLET-10**  
Bind the response to the request parameters, including nonce and audience or equivalent recipient binding, where required by the selected profile or format.

**APT-PRES-WALLET-11**  
Submit the presentation response to the Verifier endpoint indicated by the request or profile.

**APT-PRES-WALLET-12**  
Support selective disclosure where the credential format in use supports it.

A Wallet conforming to this RFC MUST NOT:

**APT-PRES-WALLET-13**  
Accept invalid or unsupported requests without clear error handling.

**APT-PRES-WALLET-14**  
Release claims not requested by the Verifier, unless explicitly authorised by the Holder and permitted by the applicable profile.

**APT-PRES-WALLET-15**  
Perform silent or automatic consent for requests that require user approval.

---

### **7.2 Verifier Requirements**

A Verifier conforming to this RFC MUST:

**APT-PRES-VER-01**  
Support OpenID4VP v1.0 for credential presentation requests and responses.

**APT-PRES-VER-02**  
Support same-device presentation flows.

**APT-PRES-VER-03**  
Support cross-device presentation flows.

**APT-PRES-VER-04**  
Create a Presentation Request containing the parameters necessary for the Wallet to process the request.

**APT-PRES-VER-05**  
Use a nonce or equivalent anti-replay mechanism.

**APT-PRES-VER-06**  
Use audience or recipient binding where required by the selected profile.

**APT-PRES-VER-07**  
Provide a response endpoint or equivalent response handling mechanism.

**APT-PRES-VER-08**  
Validate all received presentation responses before relying on them.

**APT-PRES-VER-09**  
Validate, as applicable:

* request-response correlation  
* nonce binding  
* audience binding  
* proof integrity  
* credential or presentation authenticity  
* disclosure integrity  
* holder binding, where required  
* satisfaction of the requested constraints

**APT-PRES-VER-10**  
Return a clear outcome indicating success or failure.

**APT-PRES-VER-11**  
Request only data necessary for the intended purpose.

**APT-PRES-VER-12**  
Support interoperable invocation methods for both same-device and cross-device presentation.

Where a HAIP-aligned mode is selected, a Verifier SHOULD additionally:

**APT-PRES-VER-13**  
Use signed request objects.

**APT-PRES-VER-14**  
Publish verifier metadata where supported by the ecosystem profile.

A Verifier conforming to this RFC MUST NOT:

**APT-PRES-VER-15**  
Disable nonce validation where nonce usage is required.

**APT-PRES-VER-16**  
Request unnecessary personal data beyond the needs of the use case.

---

## **8\. Interface Definitions**

### **8.1 Wallet Invocation Interface**

**Direction:** Verifier → Wallet  
**Purpose:** Invoke a Wallet for presentation

APTITUDE does not mandate a single invocation mechanism across all pilots. Implementations MUST support interoperable invocation methods appropriate to the use case and deployment context.

Supported invocation methods MAY include:

* QR codes  
* HTTPS links  
* deep links  
* custom URI schemes  
* platform wallet invocation methods

Where a custom URI scheme is used, it SHOULD be documented by the implementation profile applying this RFC.

---

### **8.2 Presentation Request Interface**

The Presentation Request MUST contain sufficient information for the Wallet to:

* identify the Verifier  
* understand what credential(s) or claim(s) are being requested  
* determine where and how to submit the response  
* bind the response to the request  
* determine whether the request is still valid

The request SHOULD include, as applicable:

* `client_id` or equivalent verifier identifier  
* `nonce`  
* `aud`  
* response endpoint information  
* credential query parameters  
* proof requirements  
* expiry  
* integrity protection, where required

---

### **8.3 Presentation Response Interface**

**Direction:** Wallet → Verifier  
**Method:** As defined by the OpenID4VP flow and profile

The presentation response MUST contain the proof material and payload required by the selected profile and credential format.

Typical response elements MAY include:

* `vp_token`  
* `presentation_submission`  
* disclosures  
* proof binding data  
* state or transaction correlation data

Example conceptual response:

{  
  "vp\_token": "\<presentation-response\>",  
  "presentation\_submission": "\<submission-object\>"  
}

Error responses SHOULD provide machine-readable and human-readable information where appropriate.

Example conceptual error:

{  
  "error": "invalid\_presentation",  
  "error\_description": "Presentation validation failed"  
}

---

### **8.4 Verifier Metadata Interface**

Where supported by the selected deployment profile, the Verifier SHOULD publish metadata describing its capabilities and requirements.

This MAY include:

* supported presentation formats  
* supported credential query methods  
* supported proof mechanisms  
* verifier signing keys or trust anchors  
* supported response modes  
* endpoint information

---

## **9\. Privacy and Security Considerations**

Implementations conforming to this RFC SHOULD follow ARF-aligned and OpenID4VP security and privacy practices.

In particular:

* Verifiers SHOULD request only the minimum data necessary.  
* Wallets SHOULD support selective disclosure where possible.  
* Nonces and expiry values SHOULD be used to reduce replay risk.  
* Request integrity protection SHOULD be used where required by the selected assurance profile.  
* Verifiers SHOULD authenticate their requests in a way that allows Wallets to assess trust.  
* Implementers SHOULD ensure secure transport and protection of response endpoints.  
* Cross-device invocation mechanisms SHOULD minimise phishing and request substitution risks.

---

## **10\. Conformance**

An implementation conforms to this RFC as a **Wallet** if it:

1. satisfies all applicable Wallet requirements in Section 7.1  
2. implements the relevant interfaces in Section 8  
3. supports same-device and cross-device presentation  
4. supports OpenID4VP v1.0 presentation processing

An implementation conforms to this RFC as a **Verifier** if it:

1. satisfies all applicable Verifier requirements in Section 7.2  
2. implements the relevant interfaces in Section 8  
3. supports same-device and cross-device presentation  
4. validates presentation responses before relying on them

### 10.1 Scope of the Conformance Matrix

This conformance matrix verifies baseline protocol interoperability between a Wallet Unit under test and the reference verifier endpoints defined by this APTITUDE Presentation Profile. It is intended to confirm correct support for representative OpenID4VP v1.0 presentation flows covered by this RFC.

The matrix is a protocol interoperability matrix, not a complete ARF/CIR compliance assessment. It focuses on presentation request processing, wallet invocation, presentation submission, and verifier response handling within the scope of this profile.

It does not exhaustively test broader trust, governance, privacy, supervisory, or deployment-specific obligations. Where ARF or CIR references are included, they provide traceability and alignment for the interoperability capability being exercised, rather than full normative coverage of the broader requirement domain.

---

## 11. Wallet-Under-Test Conformance Catalogue and Deployed Test Matrix

### 11.1 Conformance Check Catalogue

| Check ID | Conformance Check | What Is Verified At Runtime | ARF / CIR Linkage |
| --- | --- | --- | --- |
| `VP-CHECK-01` | Request resolution and processing | The wallet resolves the request URI or equivalent request reference and processes the request without transport or parsing failure. | CIR presentation interface support; ARF interoperability baseline |
| `VP-CHECK-02` | DCQL request satisfaction | The wallet returns a response matching the requested DCQL credential and claim constraints. | ARF controlled attribute presentation; CIR presentation request processing |
| `VP-CHECK-03` | Response correlation | The verifier can correlate the response to the stored session using response-mode-specific state and session handling. | CIR secure presentation interface; ARF transaction integrity baseline |
| `VP-CHECK-04` | Nonce binding | A recoverable `nonce` is present where expected and matches verifier session state. | ARF replay protection baseline; CIR secure presentation interface |
| `VP-CHECK-05` | SD-JWT key-binding validation | Where SD-JWT key binding is present, verifier checks `aud` and `sd_hash` consistency. | ARF proof binding and recipient restriction; CIR secure presentation interface |
| `VP-CHECK-06` | Transaction-data binding | Transaction data is preserved and evaluated in the same verifier session context as the credential query. | ARF transaction-aware presentation patterns; CIR secure presentation interface |
| `VP-CHECK-07` | Alternative verifier identification | The wallet interoperates with non-default verifier identification methods such as DID-based client identifiers. | ARF verifier interoperability; CIR presentation interface support |
| `VP-CHECK-08` | GET request-URI retrieval | The wallet successfully retrieves and processes the request using GET-based request-URI exposure. | ARF request transfer/interoperability; CIR presentation interface support |
| `VP-CHECK-09` | mdoc / mso_mdoc presentation validation | The verifier requires `vp_token` presence, successful mdoc verification, and claim-set consistency with the requested mdoc attributes. | ARF credential-format interoperability; CIR presentation interface support |

### 11.2 Deployed Test Case Matrix

| Test Case ID | Deployed Scenario | Request Profile / Variant | Checks Covered | Expected Outcome |
| --- | --- | --- | --- | --- |
| `VP-001` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1` with `family_name` claim | PID / SD-JWT selective-claim request | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05` | Successful completion of the PID / `family_name` presentation session |
| `VP-002` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1` with `family_name` claim | Second deployed PID / SD-JWT selective-claim variant | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05` | Successful completion of the PID / `family_name` presentation session |
| `VP-003` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1` with `family_name` claim | Third deployed PID / SD-JWT selective-claim variant | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05` | Successful completion of the PID / `family_name` presentation session |
| `VP-004` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1` with `family_name` claim with tx-data | PID / SD-JWT request with transaction data | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05`, `VP-CHECK-06` | Successful completion of the transaction-data PID presentation session |
| `VP-005` | DCQL-based VP request for `urn:eu.europa.ec.eudi:pid:1`, DID Web `client_id_scheme` with `direct_post` response mode | PID / SD-JWT request with DID:web verifier identification | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05`, `VP-CHECK-07` | Successful completion of the DID:web PID presentation session |
| `VP-006` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1`, DID Web `client_id_scheme`, `direct_post`, GET for request URI | PID / SD-JWT request with DID:web verifier identification and GET request retrieval | `VP-CHECK-01`, `VP-CHECK-02`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-05`, `VP-CHECK-07`, `VP-CHECK-08` | Successful completion of the DID:web PID presentation session using GET request retrieval |
| `VP-007` | DCQL-based Verifiable Presentation request for `urn:eu.europa.ec.eudi:pid:1:mso_mdoc`, X.509 `client_id_scheme`, `direct_post`, GET method for request URI | mdoc-class request with X.509 verifier identification and GET request retrieval | `VP-CHECK-01`, `VP-CHECK-03`, `VP-CHECK-04`, `VP-CHECK-08`, `VP-CHECK-09` | Successful completion of the mdoc X.509 presentation session using GET request retrieval |

## **12\. References**

1. OpenID Foundation, *OpenID for Verifiable Presentations 1.0*  
2. OpenID Foundation, *OpenID4VC High Assurance Interoperability Profile (HAIP)*  
3. Architecture and Reference Framework (ARF)  
4. APTITUDE interoperability and conformance testing materials  
5. WE BUILD Credential Presentation conformance specification used as structural reference for this RFC draft

---

A couple of important design choices I made here, compared with the WE BUILD presentation CS :

* I made **APTITUDE less prescriptive on invocation**, so it does **not lock the RFC to `openid4vp://` only**.  
* I kept **same-device and cross-device support mandatory** at baseline.  
* I made **HAIP-aligned features conditional where appropriate**, instead of hard-wiring all of them as universally mandatory.  
* I added the **requirements-to-test-case matrix** directly, since you said the RFC should drive ITB+ generation.

I can also turn this into a **WE BUILD-style markdown file with the same exact section naming and editorial tone**, so it looks like a sister RFC next to RFC-01.
